---
name: Gitosis::Web
Model::GitosisConfig:
     args:
       file: /var/gitosis-admin/gitosis.conf
Model::GitosisRepo:
      args:
        Directory: /var/gitosis-admin/
Model::SSHKeys:
      root_dir: /var/gitosis-admin/keydir
Model::Accounts:
      config: users.conf
    
